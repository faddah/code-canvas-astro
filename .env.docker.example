# Docker Production Environment Variables
# Copy this file to .env.docker and customize as needed

# Database path (inside Docker container, using volume mount)
DATABASE_URL=file:/app/data/taskManagement.db

# Application port
PORT=3000

# Node environment
NODE_ENV=production

# Host binding (0.0.0.0 allows external access)
HOST=0.0.0.0
